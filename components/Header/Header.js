import React, { Component } from 'react';
import { FlatList, ScrollView, Text, View, TouchableHighlight, StyleSheet  } from 'react-native';
export default class Header extends Component {


    constructor(props) {
        super(props);
    };
  render() {
    return (
        <View style={{alignItems:'center', justifyContent: 'center', flex: 1, backgroundColor: '#ff3464'}}>
        
     </View>
    );
  }
}

const styles = StyleSheet.create({
    
}); 
